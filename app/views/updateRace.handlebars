<h1>Update Race: {{race_name}}</h1>

<div>
  <form id="updateRace">
    <fieldset>
      <legend><h3>Enter new information for {{race_name}}</h3></legend>
        <label for="race_name">Race Name:
          <input type="text" class="inputField" name="race_name" id="race_name" size="10" maxlength="10" value="{{race_name}}" required>
        </label>
        <br/>

        <label for="lifespan">Lifespan (years):
          <input type="number" class="inputField" name="lifespan" id="lifespan" min="-1" value="{{lifespan}}" required>
        </label>
        <br/>

        <label for="height">Height (meters):
          <input type="number" class="inputField" name="height" id="height" min="0" step="0.01" value="{{height}}" required>
        </label>
        <br/>

        <label for="weight">Weight (kilograms):
          <input type="number" class="inputField" name="weight" id="weight" min="0" step="0.01" value="{{weight}}" required>
        </label>
        <br/>

        <label for="speed">Speed:
          <input type="number" class="inputField" name="speed" id="speed" min="0" step="0.01" value="{{speed}}" required>
        </label>
        <br/>

        <label for="special_select">Special:</label>
        <select id="special_select">
              <option value="">--Select a special attribute--</option>
              {{#each specials}}
              <option value="{{special_id}}">{{special}}</option>
              {{/each}}
          </select>
          <br/>

        <button onclick="updateRow('Races', {{race_id}}, '#updateRace')">Update</button>
    </fieldset>
  </form>
</div>